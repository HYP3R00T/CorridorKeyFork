# model-hub

Model download, checksum verification, and path resolution.

::: corridorkey.ensure_model

::: corridorkey.default_checkpoint_path

## Constants

`MODEL_URL` - The HuggingFace download URL for the default checkpoint.

`MODEL_FILENAME` - The expected filename on disk (`CorridorKey_v1.0.pth`).
